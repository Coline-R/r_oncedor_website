{% extends 'base.html.twig' %}

{% block title %}Contact - R. Oncedor{% endblock %}

{% block main %}

<main class="main" role="main">
    <!-- breadcrumb's Thread -->
    <div class="breadcrumb_thread">
        <a href="#">Accueil</a>
        <p>></p>
        <a href="#" class="active">Contact</a>
    </div>

    {# LogIn form #}
    <div class="form_block">
        <h1>Contact</h1>
        {{ form_start(form) }}
            {{ form_row(form.name) }}
            {{ form_row(form.email) }}
            {{ form_row(form.message) }}
            {{ form_row(form.save) }}
        {{ form_end(form) }}
    </div>
</main>

{% endblock %}