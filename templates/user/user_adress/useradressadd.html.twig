{% extends 'base.html.twig' %}

{% block title %}Ajouter une adresse - R. Oncedor{% endblock %}

{% block main %}
<main class="main" role="main">

    <!-- breadcrumb's Thread -->
    <div class="breadcrumb_thread">
        <a href="{{ path('home') }}">Accueil</a>
        <p>></p>
        <a href="{{ path('user_profile') }}">Espace client</a>
        <p>></p>
        <a href="{{ path('user_address') }}" class="active">Mes adresses</a>
        <p>></p>
        <a href="#" class="active">Ajouter une adresse</a>
    </div>

    <h2>AJouter une adresse</h2>
    {% include "/user/user_adress/_useradressform.html.twig" %}
</main>
{% endblock %}