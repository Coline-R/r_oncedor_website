{% extends 'base.html.twig' %}

{% block title %}Administration - Types - R. Oncedor{% endblock %}

{% block main %}
    <main class="main" role="main">

        <!-- breadcrumb's Thread -->
        <div class="breadcrumb_thread">
            <a href="{{ path('home') }}">Accueil</a>
            <p>></p>
            <a href="{{ path('admin_product') }}">Administration</a>
            <p>></p>
            <a href="#" class="active">Gestion des types</a>
        </div>

        <h1>Gestion des types</h1>

        <div class="admin_block">
            {% include 'admin/admin_includes/_admin_menu.html.twig' %}
            <div class="admin_content">
                <div>
                    <a href="{{path('admin_type_add')}}" class="btn_link">ajouter</a>
                </div>
                <table>
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Nom</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for type in types %}
                            <tr>
                                <th>{{type.id}}</th>
                                <th>{{type.name}}</th>
                                <th>
                                    <a href="{{ path('admin_type_edit', { id: type.id }) }}" class="btn_link">Editer</a>
                                    <a href="{{ path('admin_type_delete', { id: type.id }) }}" class="btn_link" onclick="return confirm('Etes vous sûre de vouloir supprimer {{type.name}} ? ')">Supprimer</a>
                                </th>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            <div>
        </div>
    </main>
{% endblock %}

