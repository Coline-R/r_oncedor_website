{% extends 'base.html.twig' %}

{% block title %}Administration - Types - R. Oncedor{% endblock %}

{% block main %}
    <main class="main" role="main">

    <!-- breadcrumb's Thread -->
    <div class="breadcrumb_thread">
        <a href="{{ path('home') }}">Accueil</a>
        <p>></p>
        <a href="{{ path('admin_product') }}">Administration</a>
        <p>></p>
        <a href="{{ path('admin_type') }}">Gestion des types</a>
        <p>></p>
        <a href="#" class="active">Ajouter un type</a>
    </div>

    <h1>Ajouter un type</h1>
        {% include 'admin/admin_includes/_type_form.html.twig' %}
    </main>
{% endblock %}