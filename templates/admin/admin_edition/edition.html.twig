{% extends 'base.html.twig' %}

{% block title %}Administration - Editions - R. Oncedor{% endblock %}

{% block main %}
    <main class="main" role="main">

        <!-- breadcrumb's Thread -->
        <div class="breadcrumb_thread">
            <a href="{{ path('home') }}">Accueil</a>
            <p>></p>
            <a href="{{ path('admin_product') }}">Administration</a>
            <p>></p>
            <a href="#" class="active">Gestion des éditions</a>
        </div>

        <h1>Gestion des éditions</h1>

        {% for message in app.flashes('info') %}
            <div class="alert alert-success">
                {{message}}
            </div>
        {% endfor %}

        <div class="admin_block">
            {% include 'admin/admin_includes/_admin_menu.html.twig' %}
            <div class="admin_content">
                <div>
                    <a href="{{path('admin_edition_add')}}" class="btn">ajouter</a>
                </div>
                <table>
                    <thead>
                        <tr>
                            <td>#</td>
                            <td>Nom des éditions</td>
                            <td>Actions</td>
                        </tr>
                    </thead>
                    <tbody>
                        {% for edition in editions %}
                            <tr>
                                <td>{{edition.id}}</td>
                                <td>{{edition.name}}</td>
                                <td>
                                    <a href="{{ path('admin_edition_edit', { id: edition.id }) }}" class="btn">Editer</a>
                                    <a href="{{ path('admin_edition_delete', { id: edition.id }) }}" class="btn" onclick="return confirm('Etes vous sûre de vouloir supprimer {{edition.name}} ? ')">Supprimer</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            <div>
        </div>
    </main>
{% endblock %}

