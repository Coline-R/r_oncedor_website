{% extends 'base.html.twig' %}

{% block title %}Administration - Editions - R. Oncedor{% endblock %}

{% block main %}
    <main class="main" role="main">

        <!-- breadcrumb's Thread -->
        <div class="breadcrumb_thread">
            <a href="{{ path('home') }}">Accueil</a>
            <p>></p>
            <a href="{{ path('admin_product') }}">Administration</a>
            <p>></p>
            <a href="#" class="active">Gestion des éditions</a>
        </div>

        <h1>Gestion des éditions</h1>

        {% for message in app.flashes('info') %}
            <div class="alert alert-success">
                {{message}}
            </div>
        {% endfor %}

        <div class="admin_block">
            {% include 'admin/admin_includes/_admin_menu.html.twig' %}
            <div class="admin_content">
                <div>
                    <a href="{{path('admin_edition_add')}}" class="btn_link">ajouter</a>
                </div>
                <table>
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Nom</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for edition in editions %}
                            <tr>
                                <th>{{edition.id}}</th>
                                <th>{{edition.name}}</th>
                                <th>
                                    <a href="{{ path('admin_edition_edit', { id: edition.id }) }}" class="btn_link">Editer</a>
                                    <a href="{{ path('admin_edition_delete', { id: edition.id }) }}" class="btn_link" onclick="return confirm('Etes vous sûre de vouloir supprimer {{edition.name}} ? ')">Supprimer</a>
                                </th>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            <div>
        </div>
    </main>
{% endblock %}

